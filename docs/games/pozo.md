# pozo

Segundo juego mínimo (gate de abstracción D-8). Solo importa engine + mesh
vía presets/rooms.

```bash
npm run demo:pozo (en Z_SDK-games-library)
```

Incluye dominio puro, autoridad, MCP jugador, vista (view-kit) y
`spec/CASOS.md`. README: `packages/pozo (Z_SDK-games-library)/README.md`.

```bash
npm test -w @zeus/pozo (en Z_SDK-games-library)
npm run e2e:pozo-mcp
```
