---
"@zeus/protocol": patch
---

WP-U155: add `"types"` conditions on public JS subpath exports (`./peer-card-seat` first, then contract/roles/gates/acl/peer-card/node*) so TypeScript resolves declarations without forced `any`.
