---
'@zeus/protocol': minor
'@zeus/volumes-ops': patch
---

WP-U99: `makeIntent` requires non-empty `game` (symmetric with `makeEnvelope`); volumes-ops role gate no longer builds a wire intent via `makeIntent`.
