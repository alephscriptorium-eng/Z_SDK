# MATRIZ-RUNTIME-51 — ficha de runtime por pieza (WP-U179)

Alimenta el gate U233. Cada celda lleva cita `ruta:línea` (relativa a la raíz del repo) que un grep
resuelve, o es un negativo con el patrón exacto usado. Ninguna celda por inferencia.

## Denominador

- **Enumeración**: globs de `workspaces` del manifest raíz — `packages/engine/*`,
  `packages/editor/*`, `packages/mesh/*`, `examples/*` (`package.json:6-11`).
- **Comando usado**:
  `for d in packages/engine/*/ packages/editor/*/ packages/mesh/*/ examples/*/; do [ -f "$d/package.json" ] && echo "$d"; done | wc -l`
  → **50** miembros npm-workspace con manifest.
- **+1 pieza con manifest propio fuera de los globs**:
  `packages/mesh/operator-ui/projects/threejs-ui-lib/package.json` (`@zeus/threejs-ui-lib`),
  declarada como workspace Angular aislado del zeus npm workspace
  (`packages/mesh/operator-ui/package.json:7`).
- **Total: 51 filas** (26 engine + 1 editor + 21 mesh + 2 examples + 1 anidada).
  El censo previo (~51) coincide con 50 + 1.
- **Excluidos, con motivo**:
  - `examples/external-consumer/` y `examples/ts-registry-consumer/`: matchean el glob pero **no
    tienen package.json** (npm no los trata como workspace); se ejercitan vía scripts raíz
    (`package.json:133` y `package.json:134`).
  - `packages/engine/protocol/test/fixtures/ts-subpath-smoke/package.json`: fixture de test, no pieza.
- **Hueco inverso (dato para U233)**: el catálogo declara entradas **sin pieza en el workspace** —
  `arg-player-uno`/`arg-player-dos`/`pozo-player`/`solve-player` con `workspace: null`
  (`packages/mesh/mcp-launcher/src/catalog.mjs:183`, `:194`, `:204`, `:214` — re-medidas U265: las
  altas de U180/U181/U234 desplazaron las cuatro, que decían `:167`, `:178`, `:188`, `:198`) y 6 hojas
  `workspace: null` en el catálogo extendido (`packages/mesh/ciudad-lifecycle/src/catalog-extend.mjs:96`).
  Las 10 las **enumera el gate** en cada corrida (`declaradasSinPieza`), así que la cifra no caduca en
  silencio aunque las líneas se muevan otra vez.

## Convenciones

- **catálogo** = `CATALOG_SEED` de `packages/mesh/mcp-launcher/src/catalog.mjs` (único catálogo de
  flota del repo; su extensión: `packages/mesh/ciudad-lifecycle/src/catalog-extend.mjs`).
- **La columna `catálogo` la sostiene un gate, no un grep de una sola vez** (U265). `matriz-51`
  contrasta cada celda contra el catálogo vivo en cada corrida y falla si divergen:
  `contraste-catalogo-caduco` (dice «sin entrada» y hay entrada, o al revés; o el `kind`/`health`
  anotado ya no es el declarado), `-incompleto` (afirma entrada sin nombrar todos los ids o sin
  anotar `kind`/`health`), `-ilegible` (celda sin claim reconocible — no se supone «negativo»),
  `-mixto` (entradas de una pieza que no coinciden en `kind`/`healthPath`). Formato de la celda
  afirmativa: ``**sí**: `<id>` [+ `<id>`…] · kind `<mcp|service>` · health `<ruta>` · `<cita>` ``.
  `kind` **ausente = `mcp`** por el typedef `CatalogEntry` del propio catálogo; `healthPath` ausente
  = `/mcp/health` por `resolveCatalog`. De ahí salen el `tipo` y el `health` que publica el gate:
  **la presencia en el catálogo NO implica `tipo: MCP`** — lo implica `kind: 'mcp'`.
  Las 16 celdas afirmativas se re-midieron el **2026-08-02** (U265); 9 de ellas decían «grep → 0» o
  «sin entrada» y llevaban caducadas desde U234/U180/U181, y las 7 restantes citaban líneas movidas.
- **env** = `packages/engine/presets-sdk/src/env/index.mjs` (fuente única de puertos: defaults MCP
  `:36-48`, overrides `ZEUS_MCP_*` `:51-67`; defaults UI/mesh `:70-90`, overrides `ZEUS_PORT_*`
  `:93-107`; `resolveAppPort` `:254-257`).
- **Negativos**: `no · grep '<patrón>' → 0` significa que el patrón (case-insensitive donde se
  indica) no tiene ocurrencias en el directorio del paquete (excluyendo `node_modules`/`dist`),
  verificado el 2026-07-31. Patrones usados por columna:
  - peercard: `peer.?card` (-i, dir completo del paquete)
  - engine/mesh: `@zeus/(rooms|socket-core|room-client-browser)|scriptorium|SocketClient|SocketServer` (src)
  - puerto: `\.listen\(|resolveZeus[A-Za-z]*Ports|ZEUS_(MCP|PORT)_` (src/bin/serve/launch)
  - disco: `VOLUMES|DISK_0[0-9]|writeFile|mkdir|appendFile|resolve[A-Za-z]*BasePath` (src/bin/serve/launch;
    para los dudosos se repitió a dir completo añadiendo `readFile|readdir`)
  - comando: `"start":` en el package.json del paquete

## Resumen de `<pendiente>` por columna

**0 celdas `<pendiente>`.** Todos los huecos son negativos verificados con el patrón de grep
declarado (no inferencia). El hueco honesto relevante para U233 no está en las celdas sino en el
inverso: 4+6 entradas de catálogo sin pieza en el workspace (ver Denominador).

Las cifras por columna **no son todas de la misma fecha**, y decirlo importa más que redondearlas:

- **catálogo: 35/51 sin entrada** (16 con entrada). Re-medida el 2026-08-02 (U265) y **sostenida por
  el gate**: `node scripts/gates/matriz-51.mjs --json` la deriva en cada corrida. Antes decía
  **44/51**, cifra de U179 que las altas de U234 (3), U180 (1) y U181 (4) dejaron rancia sin que
  nada avisara — es el mismo defecto que U265 cierra, sólo que en la cabecera.
- **comando: 33/51 sin `"start":` propio** con el patrón declarado abajo
  (`grep -l '"start":'` sobre el `package.json` de cada una de las 51). Re-medida el 2026-08-02
  (U265). Antes decía **20/51**, que no se reproduce con el patrón declarado; el número más cercano
  a esa lectura es el de piezas que el gate tipa `lib` (24/51, también derivado).
- **puerto 30/51 · peercard 38/51** (1 caso solo-fixture: `http-contract`) **· disco 24/51**:
  medidas el **2026-07-31** (U179) y **NO re-medidas por U265** — quedan fuera de la causa de este
  cambio y **ningún gate las sostiene**, así que léanse con su fecha, no como estado de hoy.

---

## packages/engine (26 filas)

| Pieza | tipo | catálogo | engine/mesh | comando | puerto | disco | deps @zeus (runtime) | peercard |
|---|---|---|---|---|---|---|---|---|
| @zeus/acta-kit | lib pura (`packages/engine/acta-kit/package.json:4`) | no · grep `acta` en `packages/mesh/mcp-launcher/src/catalog.mjs` → 0 | no · grep rooms/socket → 0 en src | no se arranca · grep `"start":` → 0 en `packages/engine/acta-kit/package.json` | n/a · grep listen → 0 | sin señal · grep disco → 0 | @zeus/protocol (`packages/engine/acta-kit/package.json:24`) | no · grep -i `peer.?card` → 0 |
| @zeus/app-shell | lib bootstrap UI (`packages/engine/app-shell/package.json:4`) | no · grep `app-shell` en catalog.mjs → 0 | mesh: secreto scriptorium vía @zeus/rooms (`packages/engine/app-shell/src/create-app-config.mjs:26`) | no se arranca (lib) · grep `"start":` → 0 | n/a escucha; resuelve puertos de apps: `packages/engine/app-shell/src/create-app-config.mjs:184` (resolveAppPort, env:254-257) | escribe config de app (`packages/engine/app-shell/src/create-app-config.mjs:274`, `:293`); dataDir default (`:189`) | presets-sdk, rooms, ui-kit (`packages/engine/app-shell/package.json:41-43`) | no · grep → 0 |
| @zeus/authority-kit | lib autoridad (`packages/engine/authority-kit/package.json:4`) | no · grep → 0 | mesh: import diferido @zeus/rooms (`packages/engine/authority-kit/src/create-authority.mjs:113`, `:237`) | no se arranca (lib) · grep `"start":` → 0 | n/a · grep listen → 0 | sin señal · grep disco → 0 | protocol, rooms (`packages/engine/authority-kit/package.json:25-26`) | **emite**: `packages/engine/authority-kit/src/issue-peer-card.mjs:62`; endpoint peer-card (`src/create-authority.mjs:144-154`) |
| @zeus/embajador-kit | lib peer-credential (`packages/engine/embajador-kit/package.json:4`) | no · grep → 0 | no · grep rooms/socket → 0 | no se arranca (lib) | n/a | sin señal | protocol (`packages/engine/embajador-kit/package.json:22`) | **emite+consume**: `packages/engine/embajador-kit/src/emitir.mjs:34`, `src/consumir.mjs:100-103`; contrato `src/tipos.mjs:2` |
| @zeus/feed-kit | lib + CLI (bin `zeus-feed-jetstream-sync` `packages/engine/feed-kit/package.json:15-17`; desc `:4`) | no · grep → 0 | no socket; gossip vía SSB MCP (`packages/engine/feed-kit/src/gossip-real.mjs:2`) | raíz `package.json:44` → `packages/engine/feed-kit/package.json:20` (`sync:jetstream`) | n/a (no escucha) · grep listen → 0 | escribe DISK_01/FIREHOSE (`packages/engine/feed-kit/src/jetstream-sync.mjs:81`, `:120`, `:146`); volumeHints (`src/families.mjs:14-24`) | firehose-core, linea-kit, presets-sdk, volumes-ops (`packages/engine/feed-kit/package.json:27-30`) | no · grep → 0 |
| @zeus/firehose-core | lib pura, «no MCP server» (`packages/engine/firehose-core/package.json:4`) | no · grep → 0 | no · grep → 0 | no se arranca (lib) | n/a | lee volumen firehose (`packages/engine/firehose-core/src/browse.mjs:81` readdir) | presets-sdk (`packages/engine/firehose-core/package.json:26`) | no · grep → 0 |
| @zeus/game-engine | lib motor puro (`packages/engine/game-engine/package.json:4`) | no · grep → 0 | **es el engine** («Pure-logic gamemap engine» `packages/engine/game-engine/package.json:4`) | no se arranca (lib); test raíz `package.json:54` | n/a | sin señal | protocol (`packages/engine/game-engine/package.json:25`) | no · grep → 0 |
| @zeus/http-contract | lib contrato HTTP (`packages/engine/http-contract/package.json:4`) | no · grep → 0 | no socket · grep → 0 | no se arranca (lib); test raíz `package.json:49` | n/a escucha; proyecta servers[] OpenAPI desde env (`packages/engine/http-contract/src/endpoint.mjs:23`) | sin señal | presets-sdk (`packages/engine/http-contract/package.json:31`) | no en src; solo fixture `packages/engine/http-contract/test/fixtures/ts-grafo-cercano-smoke/consumer-a.ts` |
| @zeus/lifecycle-kit | lib XState (`packages/engine/lifecycle-kit/package.json:4`) | no · grep → 0 | no · grep → 0 | no se arranca (lib) | n/a | sin señal | ninguna @zeus (dependencies = xstate `packages/engine/lifecycle-kit/package.json:21-23`) | no · grep → 0 |
| @zeus/linea-kit | lib + CLI (bin `zeus-linea-kit` `packages/engine/linea-kit/package.json:7-9`) | no · grep → 0 | no · grep → 0 | CLI por bin (`packages/engine/linea-kit/package.json:7-9`); sin start | n/a | loader fs DISK_02/LINEAS (`packages/engine/linea-kit/src/loader.mjs:2`, `:222`); DISK_03/FORCES (`src/forces-loader.mjs:2`, `:121`) | ninguna @zeus (dependencies = ajv/yaml `packages/engine/linea-kit/package.json:29-32`) | no · grep → 0 |
| @zeus/parte-kit | lib pura (`packages/engine/parte-kit/package.json:4`) | no · grep → 0 | no · grep → 0 | no se arranca (lib) | n/a | sin señal | protocol (`packages/engine/parte-kit/package.json:24`) | no · grep → 0 |
| @zeus/playbook-kit | lib + CLI (bin `zeus-playbook-run` `packages/engine/playbook-kit/package.json:11-13`) | no · grep → 0 | no socket; cliente MCP (`packages/engine/playbook-kit/src/mcp-client.mjs:10`) | CLI `bin/run-playbook.mjs` (`packages/engine/playbook-kit/package.json:11-13`); test raíz `package.json:66` | no escucha; cliente hacia argPlayer.uno vía `--port` (`packages/engine/playbook-kit/bin/run-playbook.mjs:8`, `:66`) | escribe acta outFile (`packages/engine/playbook-kit/bin/run-playbook.mjs:82`) | presets-sdk (`packages/engine/playbook-kit/package.json:22`) | no · grep → 0 |
| @zeus/player-mcp-kit | lib patrón MCP-por-actor (`packages/engine/player-mcp-kit/package.json:4`) | no · grep → 0 | mesh: room bridge sobre @zeus/rooms (`packages/engine/player-mcp-kit/src/room-bridge.mjs:10`) | no se arranca (lib); test raíz `package.json:68` | n/a | sin señal | presets-sdk, rooms (`packages/engine/player-mcp-kit/package.json:21-22`) | **porta**: `packages/engine/player-mcp-kit/src/room-bridge.mjs:135-141` (PeerCardForConnect) |
| @zeus/presets-sdk | lib/SDK «MCP catalog & presets» (`packages/engine/presets-sdk/package.json:4`) | sin entrada; **fuente de puertos del catálogo** (`packages/mesh/mcp-launcher/src/catalog.mjs:8-9`) | define slots UI/mesh, incl. scriptorium (`packages/engine/presets-sdk/src/env/index.mjs:70-90`, `:87`) | no se arranca (lib); test raíz `package.json:38` | no escucha; **define** defaults y overrides (`packages/engine/presets-sdk/src/env/index.mjs:36-48`, `:51-67`, `:93-107`) | presets store escribe `presets.json` en dataDir (`packages/engine/presets-sdk/src/presets/store.mjs:73`, `:108`) | http-contract, ui-kit (`packages/engine/presets-sdk/package.json:62-63`) | no · grep → 0 |
| @zeus/protocol | lib contrato único (`packages/engine/protocol/package.json:4`) | no · grep → 0 | no socket · grep → 0 | no se arranca (lib); spec:generate raíz `package.json:61` | n/a | sin señal | ninguna @zeus runtime (dependencies `packages/engine/protocol/package.json:57` sin @zeus; dev http-contract `:61`) | **define/emite el formato**: makePeerCard (`packages/engine/protocol/src/peer-card.mjs:143`); verificación ACL (`src/acl.mjs:242-257`); seat (`src/peer-card-seat.mjs:19` attachTravelingSeat) |
| @zeus/reparto-kit | lib reparto/permisos (`packages/engine/reparto-kit/package.json:4`) | no · grep → 0 | no socket · grep → 0 | no se arranca (lib) | n/a | sin señal | protocol, view-kit (`packages/engine/reparto-kit/package.json:41-42`) | **verifica**: `packages/engine/reparto-kit/src/permisos.mjs:19`, `:107-118` (PeerCardFresh); doc `src/index.mjs:4` |
| @zeus/room-client-browser | lib cliente room browser (`packages/engine/room-client-browser/package.json:4`) | no · grep → 0 | mesh: cliente de /runtime (`packages/engine/room-client-browser/src/index.mjs:4`; `browser/dev-room-config.mjs:8` scriptoriumUrl) | no se arranca (lib) | n/a escucha; resuelve puertos UI (`packages/engine/room-client-browser/src/index.mjs:3`, `:29`) | sin señal | presets-sdk, rooms (`packages/engine/room-client-browser/package.json:25-26`) | no · grep → 0 |
| @zeus/rooms | lib cliente rooms E1 (`packages/engine/rooms/package.json:4`) | no · grep → 0 | mesh: room `scriptorium.<sessionId>` / ZEUS_SCRIPTORIUM_ROOM (`packages/engine/rooms/src/config.mjs:35-36`) | no se arranca (lib); test `packages/engine/rooms/package.json:14-16` | n/a escucha (cliente) | sin señal | socket-core (`packages/engine/rooms/package.json:18`) | **porta**: peerCard en CLIENT_REGISTER (`packages/engine/rooms/src/index.mjs:52-53`, `:70-72`) |
| @zeus/socket-core | lib superficie Socket.IO (`packages/engine/socket-core/package.json:4`) | no · grep → 0 | **es la superficie socket de la mesh** (`packages/engine/socket-core/src/client.mjs:2`) | no se arranca (lib); test raíz `package.json:51` | n/a · grep `listen` → 0 en src (el bind lo hace socket-server) | sin señal | ninguna @zeus (dependencies = socket.io `packages/engine/socket-core/package.json:25-29`) | no · grep → 0 |
| @zeus/story-board-schema | lib schema+AJV (`packages/engine/story-board-schema/package.json:4`) | no · grep → 0 | no · grep → 0 | no se arranca (lib) | n/a | sin señal | ninguna @zeus (dependencies = ajv `packages/engine/story-board-schema/package.json:21-23`) | no · grep → 0 |
| @zeus/test-utils | lib helpers de smoke (`packages/engine/test-utils/package.json:4`) | no · grep → 0 | no · grep → 0 | no se arranca (lib) | n/a | crea VOLUMES mínimo temporal / ZEUS_VOLUMES_ROOT (`packages/engine/test-utils/src/smoke-env.mjs:2`, `:5`, `:10-14`) | http-contract (`packages/engine/test-utils/package.json:18`) | no · grep → 0 |
| @zeus/ui-3d-kit | lib three.js (`packages/engine/ui-3d-kit/package.json:4`) | no · grep → 0 | engine: dep @zeus/game-engine (`packages/engine/ui-3d-kit/package.json:26`) | no se arranca (lib); test raíz `package.json:53` | n/a | sin señal | game-engine, protocol (`packages/engine/ui-3d-kit/package.json:26-27`) | no · grep → 0 |
| @zeus/ui-kit | lib UI shell (`packages/engine/ui-kit/package.json:4`) | no · grep → 0 | no · grep → 0 | no se arranca (lib) | n/a | sin señal | http-contract (`packages/engine/ui-kit/package.json:34`; presets-sdk solo dev `:31`) | no · grep → 0 |
| @zeus/view-kit | lib vistas 3D+HTML con room wiring (`packages/engine/view-kit/package.json:4`) | no · grep → 0 | mesh: suscripción a canales de room / ROOM_MESSAGE (`packages/engine/view-kit/src/channel-events.mjs:2`, `:6-8`) | no se arranca (lib); test raíz `package.json:67` | n/a | sin señal | protocol, ui-3d-kit (`packages/engine/view-kit/package.json:25-26`) | no · grep → 0 |
| @zeus/volumes-ops | lib + servidor REST embebible, MCP proyectado (`packages/engine/volumes-ops/package.json:4`; `src/server.mjs:1-3`) | no · grep → 0 | no socket · grep → 0 | sin start; factoría `createVolumesOpsServer` (`packages/engine/volumes-ops/src/server.mjs:18`) | puerto del llamador, default 0 efímero, «no hardcoded mesh port» (`packages/engine/volumes-ops/src/server.mjs:3`, `:19`) | mide/purga VOLUMES con ledger (`packages/engine/volumes-ops/src/ledger.mjs:17-18`, `:34`; `src/counters.mjs:6`) | http-contract, linea-kit, presets-sdk, protocol (`packages/engine/volumes-ops/package.json:21-24`) | no · grep → 0 |
| @zeus/webrtc-signaling | lib señalización WebRTC (`packages/engine/webrtc-signaling/package.json:4`) | no · grep → 0 | mesh: señalización sobre rooms socket (`packages/engine/webrtc-signaling/src/socket-room-signaling.mjs:20`, `:28` ZEUS_SCRIPTORIUM_URL) | no se arranca (lib); test raíz `package.json:118` | n/a escucha | sin señal | presets-sdk, protocol, rooms (`packages/engine/webrtc-signaling/package.json:30-32`) | **verifica gate**: PEER_CARD / PeerCardGatedType (`packages/engine/webrtc-signaling/src/index.mjs:47-56`) |

## packages/editor (1 fila)

| Pieza | tipo | catálogo | engine/mesh | comando | puerto | disco | deps @zeus (runtime) | peercard |
|---|---|---|---|---|---|---|---|---|
| @zeus/editor-ui | UI server Express (`packages/editor/editor-ui/package.json:4`, main `:6`) | **sí**: `editor-ui` · kind `service` (sin superficie MCP) · health `/health` · `packages/mesh/mcp-launcher/src/catalog.mjs:284-298` (workspace `:288`) — alta U181; la entrada `linea-editor` (`:138-148`) es otra pieza | engine: consume @zeus/game-engine (`packages/editor/editor-ui/src/world/materials.mjs:7`); sin cliente socket propio · grep rooms/socket → 0 en src | raíz `package.json:23` → `packages/editor/editor-ui/package.json:8` | 3012 (env:71) · cambia con ZEUS_PORT_EDITOR (env:94) vía resolveAppPort (`packages/engine/app-shell/src/create-app-config.mjs:184`); default `packages/editor/editor-ui/src/config.mjs:17`; listen `src/server.mjs:210` | escribe `world-draft.json` en dataDir (`packages/editor/editor-ui/src/world/draft-store.mjs:5`, `:18`, `:45`, `:52`) | app-shell, game-engine, http-contract, linea-kit, playbook-kit, presets-sdk, story-board-schema, test-utils, ui-kit (`packages/editor/editor-ui/package.json:22-30`) | no · grep → 0 |

## packages/mesh (21 filas)

| Pieza | tipo | catálogo | engine/mesh | comando | puerto | disco | deps @zeus (runtime) | peercard |
|---|---|---|---|---|---|---|---|---|
| @zeus/3d-monitor | UI server Express 3D (`packages/mesh/3d-monitor/package.json:4`) | **sí**: `3d-monitor` · kind `service` (sin superficie MCP) · health `/health` · `packages/mesh/mcp-launcher/src/catalog.mjs:329-343` (workspace `:333`) — alta U181 | mesh: portal sobre el runtime scriptorium; sirve room-client assets (`packages/mesh/3d-monitor/src/server.mjs:6`, `:27`, `:77`) | raíz `package.json:31` → `packages/mesh/3d-monitor/package.json:8` | 3019 (env:76) · ZEUS_PORT_DEBUG_3D (env:99); default `packages/mesh/3d-monitor/src/config.mjs:14`; listen `src/server.mjs:150` | sin fs propio · grep disco+readFile → 0 en src; config vía app-shell (dep `packages/mesh/3d-monitor/package.json:17`; escritura `packages/engine/app-shell/src/create-app-config.mjs:274`) | app-shell, presets-sdk, room-client-browser, ui-3d-kit, ui-kit, view-kit (`packages/mesh/3d-monitor/package.json:17-22`) | no · grep → 0 |
| @zeus/blob-sync-harness | harness de validación U100 (`packages/mesh/blob-sync-harness/package.json:4`) | no · grep → 0 | mesh indirecta: señalización de @zeus/webrtc-signaling (`packages/mesh/blob-sync-harness/src/run-spike.mjs:8-14`) | raíz `package.json:112` → `packages/mesh/blob-sync-harness/package.json:12` (`spike`) | n/a · grep listen → 0 en src | sin escritura VOLUMES · grep write → 0; cid compatible con manifiestos VOLUMES (`packages/mesh/blob-sync-harness/src/content-address.mjs:17`) | authority-kit, protocol, webrtc-signaling (`packages/mesh/blob-sync-harness/package.json:17-19`) | **emite y verifica LAN-gate**: `packages/mesh/blob-sync-harness/src/run-spike.mjs:6-7`; `src/lan-gate.mjs:4`, `:16-23` |
| @zeus/blobstore-client | cliente HTTP de /x/blobstore/v0 + fixture (`packages/mesh/blobstore-client/package.json:4`) | no · grep → 0 | no socket directo · grep rooms → 0 en src; dep webrtc-signaling (`packages/mesh/blobstore-client/package.json:18`) | raíz `package.json:114` → `packages/mesh/blobstore-client/package.json:12` (`fixture`) | fixture escucha efímero `listen(0)` (`packages/mesh/blobstore-client/src/run-fixture.mjs:28`; `src/fixture-sidecar.mjs:174`) | valida cid/manifest VOLUMES (`packages/mesh/blobstore-client/src/volumes-cid.mjs:2`; `src/run-fixture.mjs:39` DISK_04/SSB/manifest.json) | blob-sync-harness, webrtc-signaling (`packages/mesh/blobstore-client/package.json:17-18`; dev authority-kit/protocol `:21-22`) | **verifica LAN**: `packages/mesh/blobstore-client/src/lan.mjs:16-20`; `src/invariants.mjs:76` |
| @zeus/cache-browser | UI server Express (`packages/mesh/cache-browser/package.json:4`) | **sí**: `cache-browser` · kind `service` (sin superficie MCP) · health `/health` · `packages/mesh/mcp-launcher/src/catalog.mjs:258-270` (workspace `:262`) — alta U234 | mesh: suscriptor de track ARG vía rooms (`packages/mesh/cache-browser/src/arg-track-subscriber.mjs:7`) | raíz `package.json:26` → `packages/mesh/cache-browser/package.json:8` | 3015 (env:73) · ZEUS_PORT_VIEW (env:96); default `packages/mesh/cache-browser/src/config.mjs:16`; listen `src/server.mjs:179` | lee DISK_02/LINEAS vía loader de linea-system (`packages/mesh/cache-browser/src/browse-bridge.mjs:9`, `:37-42`) | app-shell, http-contract, linea-system, presets-sdk, rooms, test-utils, ui-kit (`packages/mesh/cache-browser/package.json:23-29`) | no · grep → 0 |
| @zeus/ciudad-lifecycle | MCP server (composición/brain) (`packages/mesh/ciudad-lifecycle/src/server.mjs:1-2`; `package.json:4`) | **sí**: `ciudad-lifecycle` · kind `mcp` (ausente = default) · health `/mcp/health` · `packages/mesh/mcp-launcher/src/catalog.mjs:232-244` (workspace `:235`) — alta U180; además **extiende** el catálogo con 6 hojas `workspace: null` (`packages/mesh/ciudad-lifecycle/src/catalog-extend.mjs:6`, `:96`) | no socket · grep → 0; brain sobre ProcessManager del launcher (`packages/mesh/ciudad-lifecycle/src/server.mjs:6`) | raíz `package.json:22` → `packages/mesh/ciudad-lifecycle/package.json:14` | 3051 default (`packages/mesh/ciudad-lifecycle/src/server.mjs:13`) · ZEUS_MCP_CIUDAD_LIFECYCLE (`src/server.mjs:16`); NOTA: fuera del mapa env de presets-sdk (grep `CIUDAD` en env → 0) | sin señal · grep disco+readFile → 0 en src | lifecycle-kit, mcp-launcher, presets-sdk (`packages/mesh/ciudad-lifecycle/package.json:23-25`) | no · grep → 0 |
| @zeus/console-monitor | monitor TUI + MCP server (`packages/mesh/console-monitor/package.json:4`; MCP `src/mcp-server.mjs:7`) | **sí**: `console-monitor` · kind `mcp` (ausente = default) · health `/mcp/health` · `packages/mesh/mcp-launcher/src/catalog.mjs:170-179` (workspace `:173`) | mesh: cliente de sesión sobre scriptorium URL (`packages/mesh/console-monitor/src/config.mjs:40-46`) | raíz `package.json:25` → `packages/mesh/console-monitor/package.json:8` | MCP 3014 (env:44 playerDebug.monitor) · ZEUS_PORT_PLAYER_DEBUG (env:62; `packages/mesh/console-monitor/src/config.mjs:25`) | sin señal · grep disco+readFile → 0 en src | app-shell, presets-sdk (`packages/mesh/console-monitor/package.json:17-18`) | no · grep → 0 |
| @zeus/firehose-browser | UI server Express (`packages/mesh/firehose-browser/package.json:4`) | **sí**: `firehose-browser` · kind `service` (sin superficie MCP) · health `/health` · `packages/mesh/mcp-launcher/src/catalog.mjs:271-283` (workspace `:275`) — alta U234; la entrada `firehose` (`:159-169`) es @zeus/linea-firehose — piezas y puertos distintos | mesh: rooms subscriber (`packages/mesh/firehose-browser/src/arg-track-subscriber.mjs:7`) | raíz `package.json:27` → `packages/mesh/firehose-browser/package.json:8` | 3016 (env:74) · ZEUS_PORT_FIREHOSE (env:97); default `packages/mesh/firehose-browser/src/config.mjs:14`; listen `src/server.mjs:169` | browse DISK_01 vía firehose-core (`packages/mesh/firehose-browser/src/firehose-bridge.mjs:2`, `:15`) | app-shell, firehose-core, http-contract, presets-sdk, rooms, test-utils, ui-kit (`packages/mesh/firehose-browser/package.json:22-28`) | no · grep → 0 |
| @zeus/force-system | MCP server read-only (`packages/mesh/force-system/package.json:4`) | **sí**: `forces` · kind `mcp` (ausente = default) · health `/mcp/health` · `packages/mesh/mcp-launcher/src/catalog.mjs:128-137` (workspace `:131`) | no socket · grep → 0 | raíz `package.json:18` → `packages/mesh/force-system/package.json:12` | 4113 (env:39) · ZEUS_MCP_FORCES (env:57); resolución `packages/mesh/force-system/src/start.mjs:15` | lee DISK_03/FORCES (`packages/mesh/force-system/src/force-server.mjs:2`; `src/loader.mjs:26` resolveForcesBasePath) | http-contract, linea-kit, presets-sdk (`packages/mesh/force-system/package.json:21-23`) | no · grep → 0 |
| @zeus/linea-editor | MCP server de autoría gated (`packages/mesh/linea-editor/package.json:4`) | **sí**: `linea-editor` · kind `mcp` (ausente = default) · health `/mcp/health` · `packages/mesh/mcp-launcher/src/catalog.mjs:138-148` (workspace `:141`) | no socket · grep → 0 | raíz `package.json:21` → `packages/mesh/linea-editor/package.json:15` | 4115 (env:41) · ZEUS_MCP_LINEA_EDITOR (env:59); resolución `packages/mesh/linea-editor/src/start.mjs:31` | base VOLUMES/LINEAS (`packages/mesh/linea-editor/src/start.mjs:18-19`); escribe story-board exportado (`src/export-story-board.mjs:169`) | http-contract, linea-kit, presets-sdk, reparto-kit, story-board-schema (`packages/mesh/linea-editor/package.json:24-28`) | **verifica** (gate peer-card + ZEUS_MCP_APPROVAL_TOKEN): `packages/mesh/linea-editor/src/editor-server.mjs:52`, `:60`, `:91`, `:200-239` |
| @zeus/linea-firehose | MCP server firehose (`packages/mesh/linea-firehose/package.json:4`; `src/start.mjs:2`) | **sí**: `firehose` · kind `mcp` (ausente = default) · health `/mcp/health` · `packages/mesh/mcp-launcher/src/catalog.mjs:159-169` (workspace `:162`) | no socket · grep → 0; enlaza la UI firehose vía env (`packages/mesh/linea-firehose/src/logic.mjs:175`) | raíz `package.json:28` → `packages/mesh/linea-firehose/package.json:8` | 3008 (env:42) · ZEUS_MCP_FIREHOSE (env:60; `packages/mesh/linea-firehose/src/config.mjs:12`) | lee VOLUMES/DISK_01/FIREHOSE (`packages/mesh/linea-firehose/src/logic.mjs:90`, `:183`) | firehose-core, presets-sdk (`packages/mesh/linea-firehose/package.json:27-28`) | no · grep → 0 |
| @zeus/linea-system | MCP servers (espana + wp/historia) (`packages/mesh/linea-system/package.json:4`) | **sí**: `linea-espana` + `linea-wp-historia` · kind `mcp` (ausente = default) · health `/mcp/health` · `packages/mesh/mcp-launcher/src/catalog.mjs:76-86` (workspace `:79`) + `:87-97` (workspace `:90`) | no socket · grep → 0 | raíz `package.json:17` → `packages/mesh/linea-system/package.json:12` | 4111/4112 (env:38) · ZEUS_MCP_LINEA_ESPAN / ZEUS_MCP_LINEA_WP (env:55-56); resolución `packages/mesh/linea-system/src/start.mjs:12` | lee DISK_02/LINEAS (`packages/mesh/linea-system/src/loader.mjs:28` resolveLineasBasePath; cache wikitext `src/cache-wikitext.mjs:2`) | http-contract, linea-kit, presets-sdk (`packages/mesh/linea-system/package.json:21-23`) | no · grep → 0 |
| @zeus/mcp-launcher | MCP server meta-ops/actuador (`packages/mesh/mcp-launcher/package.json:4`) | **sí**: `launcher` · kind `mcp` (ausente = default) · health `/mcp/health` · `packages/mesh/mcp-launcher/src/catalog.mjs:221-231` (workspace `:224`) — alta U234 — **custodio del catálogo** y entrada de él; su puerto en `buildPortTable` (`packages/mesh/mcp-launcher/src/catalog.mjs:488`) | no socket · grep → 0 | raíz `package.json:20` → `packages/mesh/mcp-launcher/package.json:14` | 3050 (env:43) · ZEUS_MCP_LAUNCHER (env:61; `packages/mesh/mcp-launcher/src/launcher-server.mjs:14-18`) | sin persistencia: intentional stops en memoria (`packages/mesh/mcp-launcher/src/process-manager.mjs:40-41` Set) | presets-sdk (`packages/mesh/mcp-launcher/package.json:23`) | no · grep → 0 |
| @zeus/oasis-webrtc | servidor HTTP módulo /webrtc (`packages/mesh/oasis-webrtc/package.json:4`) | no · grep → 0 | no rooms directo · grep → 0 en src; señalización vía dep webrtc-signaling (`packages/mesh/oasis-webrtc/package.json:20`) | raíz `package.json:122` → `packages/mesh/oasis-webrtc/package.json:11` | 3022 (env:82) · ZEUS_PORT_OASIS_WEBRTC (`packages/mesh/oasis-webrtc/src/http-api.mjs:32`; env:103); listen `src/http-api.mjs:189` | lee assets public (`packages/mesh/oasis-webrtc/src/http-api.mjs:87-92`); sin VOLUMES · grep → 0 | presets-sdk, webrtc-signaling (`packages/mesh/oasis-webrtc/package.json:19-20`) | no · grep -i `peer.?card` → 0 en el dir |
| @zeus/operator-bridge | lib puente puro, «no network» (`packages/mesh/operator-bridge/package.json:4`) | no · grep → 0 | no socket (declarado en `packages/mesh/operator-bridge/package.json:4`; grep → 0) | no se arranca (lib); test raíz `package.json:55` | n/a | sin señal | protocol, parte-kit (`packages/mesh/operator-bridge/package.json:14-15`) | no · grep → 0 |
| @zeus/operator-ui | UI Angular tier-2 + servidor estático (`packages/mesh/operator-ui/package.json:7`) | no · grep → 0 | mesh: dev-app usa room-client-browser (`packages/mesh/operator-ui/projects/dev-app/src/app/app.ts:11`; dep `package.json:46`) | raíz `package.json:32` (node serve.mjs); build raíz `package.json:56` → `packages/mesh/operator-ui/package.json:19-21` | 3020 (env:77) · OPERATOR_UI_PORT / ZEUS_PORT_OPERATOR_UI (`packages/mesh/operator-ui/serve.mjs:65-67`; env:100); listen `serve.mjs:143` | lee dist/index.html (`packages/mesh/operator-ui/serve.mjs:131`) | operator-bridge, room-client-browser, ui-3d-kit (`packages/mesh/operator-ui/package.json:45-47`) | **verifica** en puerta `/api/puerta/enter` (`packages/mesh/operator-ui/serve.mjs:97-101`) |
| @zeus/player-3d-ui | UI server Express, visor 3D room client (`packages/mesh/player-3d-ui/package.json:4`) | **sí**: `player-3d-ui` · kind `service` (sin superficie MCP) · health `/health` · `packages/mesh/mcp-launcher/src/catalog.mjs:314-328` (workspace `:318`) — alta U181 | mesh: room client del runtime (`packages/mesh/player-3d-ui/src/server.mjs:23`, `:76`; `assets/js/event-choreographer.mjs:4`) | raíz `package.json:30` → `packages/mesh/player-3d-ui/package.json:8` | 3018 (env:75) · ZEUS_PORT_PLAYER_3D (env:98); default `packages/mesh/player-3d-ui/src/config.mjs:14`; listen `src/server.mjs:132` | sin fs propio · grep → 0; config vía app-shell (dep `packages/mesh/player-3d-ui/package.json:17`) | app-shell, presets-sdk, room-client-browser, ui-3d-kit, ui-kit, view-kit, game-engine (`packages/mesh/player-3d-ui/package.json:17-23`) | no · grep → 0 |
| @zeus/player-ui | UI server DJ deck, rol dj sobre la room (`packages/mesh/player-ui/package.json:4`) | **sí**: `player-ui` · kind `service` (sin superficie MCP) · health `/health` · `packages/mesh/mcp-launcher/src/catalog.mjs:299-313` (workspace `:303`) — alta U181 | mesh: href scriptorium (`packages/mesh/player-ui/src/config.mjs:4-5`); dep rooms (`package.json:29`) | raíz `package.json:24` → `packages/mesh/player-ui/package.json:8` | 3013 (env:72) · ZEUS_PORT_PLAYER (env:95); default `packages/mesh/player-ui/src/config.mjs:17` vía resolveAppPort (`packages/engine/app-shell/src/create-app-config.mjs:184`) | lee base LINEAS / DISK_02 (`packages/mesh/player-ui/src/aleph-bridge.mjs:9`, `:19`, `:265-270`) | app-shell, firehose-core, http-contract, presets-sdk, protocol, room-client-browser, rooms, test-utils, ui-kit (`packages/mesh/player-ui/package.json:23-31`) | no · grep → 0 |
| @zeus/socket-server | servidor runtime Socket.IO E2 (`packages/mesh/socket-server/package.json:4`) | **sí**: `socket-server` · kind `service` (sin superficie MCP) · health `/health` · `packages/mesh/mcp-launcher/src/catalog.mjs:245-257` (workspace `:249`) — alta U234 | **es la mesh**: bind HTTP+socket (`packages/mesh/socket-server/src/create-server.mjs:59`); secreto scriptorium (`src/config.mjs:2`) | raíz `package.json:29` → `packages/mesh/socket-server/package.json:14` | 3017 (env:87 scriptorium) · ZEUS_PORT_SCRIPTORIUM / ZEUS_SCRIPTORIUM_PORT (`packages/mesh/socket-server/src/config.mjs:27-32`; env:104) | lee admin-ui index.html estático (`packages/mesh/socket-server/src/admin-ui.mjs:36`); sin VOLUMES · grep → 0 | presets-sdk, rooms, socket-core (`packages/mesh/socket-server/package.json:19-21`) | no en su src · grep -i `peer.?card` → 0 (el porte va en el cliente: `packages/engine/rooms/src/index.mjs:70-72`) |
| @zeus/solar-system | MCP servers demo (`packages/mesh/solar-system/package.json:4`) | **sí**: `solar-sun` + `solar-moon` + `solar-earth` · kind `mcp` (ausente = default) · health `/mcp/health` · `packages/mesh/mcp-launcher/src/catalog.mjs:98-107` (workspace `:101`) + `:108-117` (workspace `:111`) + `:118-127` (workspace `:121`) | no socket · grep → 0 | raíz `package.json:16` → `packages/mesh/solar-system/package.json:8` | 4101/4102/4103 (env:37) · ZEUS_MCP_SUN/MOON/EARTH (env:52-54); resolución `packages/mesh/solar-system/src/start.mjs:17`; `src/body-server.mjs:22` | sin señal (datos deterministas `packages/mesh/solar-system/package.json:4`; grep fs → 0) | http-contract, presets-sdk (`packages/mesh/solar-system/package.json:22-23`) | no · grep → 0 |
| @zeus/ssb-system | exporter + MCP loader read-only (`packages/mesh/ssb-system/package.json:4`) | **sí**: `ssb` · kind `mcp` (ausente = default) · health `/mcp/health` · `packages/mesh/mcp-launcher/src/catalog.mjs:149-158` (workspace `:152`) | no socket · grep → 0 | raíz `package.json:19` → `packages/mesh/ssb-system/package.json:13`; sync raíz `package.json:43` → `packages/mesh/ssb-system/package.json:14` | 4114 (env:40) · ZEUS_MCP_SSB (env:58); resolución `packages/mesh/ssb-system/src/start.mjs:14` | lee DISK_04/SSB (`packages/mesh/ssb-system/src/loader.mjs:7`, `:19`); export escribe (`src/export.mjs:2`, `:86-100`) | linea-kit, presets-sdk (`packages/mesh/ssb-system/package.json:23-24`) | no · grep → 0 |
| @zeus/webrtc-viewer | viewer mesh WebRTC + servidor estático (`packages/mesh/webrtc-viewer/package.json:7`) | no · grep → 0 | mesh: señalización browser sobre room-client (`packages/mesh/webrtc-viewer/src/browser/browser-signaling.mjs:13`; `serve.mjs:16-19`) | raíz `package.json:121` → `packages/mesh/webrtc-viewer/package.json:16` | 3023 (env:79) · WEBRTC_VIEWER_PORT / ZEUS_PORT_WEBRTC_VIEWER (`packages/mesh/webrtc-viewer/serve.mjs:54-57`; env:102) | lee assets (`packages/mesh/webrtc-viewer/serve.mjs:130`, `:148`) | linea-kit, presets-sdk, protocol, room-client-browser, webrtc-signaling (`packages/mesh/webrtc-viewer/package.json:27-31`) | **porta**: allowLocalPeerCard / inyección peerCard (`packages/mesh/webrtc-viewer/serve.mjs:45-46`; `src/browser/browser-signaling.mjs:103-110`) |

## examples (2 filas)

| Pieza | tipo | catálogo | engine/mesh | comando | puerto | disco | deps @zeus (runtime) | peercard |
|---|---|---|---|---|---|---|---|---|
| @zeus/game-demos | demos ejecutables (`examples/game-demos/package.json:6`) | no · grep → 0 | engine+mesh: apps sobre @zeus/rooms + @zeus/game-engine (`examples/game-demos/apps/map/index.mjs:1`; `package.json:6`) | raíz `package.json:35` → `examples/game-demos/package.json:8` (`node launch.mjs`) | n/a (cliente); room desde .env ZEUS_SCRIPTORIUM_ROOM (`examples/game-demos/launch.mjs:19`) | lee .env de la raíz (`examples/game-demos/lib/load-env.mjs:1`, `:16`) | game-engine, rooms (`examples/game-demos/package.json:17-18`) | no · grep → 0 |
| @zeus/ping-pong-bots | bots demo, clientes de sesión (`examples/ping-pong-bots/package.json:6`) | no · grep → 0 | mesh: clientes del socket-server vía @zeus/rooms (`examples/ping-pong-bots/apps/ping/index.mjs:1`, `:17`) | raíz `package.json:36` → `examples/ping-pong-bots/package.json:8` (`node launch.mjs`) | n/a (clientes); identidad por env ZEUS_SCRIPTORIUM_USER (`examples/ping-pong-bots/launch.mjs:11-18`) | sin señal · grep → 0 | presets-sdk, rooms, socket-core (`examples/ping-pong-bots/package.json:22-24`) | no · grep → 0 |

## pieza anidada (1 fila)

| Pieza | tipo | catálogo | engine/mesh | comando | puerto | disco | deps @zeus (runtime) | peercard |
|---|---|---|---|---|---|---|---|---|
| @zeus/threejs-ui-lib | lib Angular Three.js (`packages/mesh/operator-ui/projects/threejs-ui-lib/package.json:4`) | no · grep `threejs` en catalog.mjs → 0 | engine: dep @zeus/ui-3d-kit (`packages/mesh/operator-ui/projects/threejs-ui-lib/package.json:25`) | no se arranca (lib); build ng desde operator-ui (`packages/mesh/operator-ui/package.json:19`, `:21`; raíz `package.json:56`) | n/a · grep listen → 0 | sin señal · grep → 0 | ui-3d-kit (`packages/mesh/operator-ui/projects/threejs-ui-lib/package.json:25`) | no · grep → 0 |
