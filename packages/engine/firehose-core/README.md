# `@zeus/firehose-core`

Biblioteca pura de firehose: schema, loader y browse de volúmenes. Sin servidor
MCP (eso vive en mesh/`linea-firehose`).

```js
import { /* browse / schema / loader */ } from '@zeus/firehose-core';
```

## Tests

`npm test -w @zeus/firehose-core`
