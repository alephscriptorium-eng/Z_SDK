# `@zeus/ui-kit`

Shell UI compartido Zeus: templates HTML, navegación, themes y assets estáticos
(`assets/`). Base de viewers HTML del mesh.

```js
import { template, ThemeHandler, assetsDir } from '@zeus/ui-kit';
```

## Tests

`npm test -w @zeus/ui-kit`
